void main(){
  int x = 5;
  int y = 28;
  
  if(x > y){
    print("max(x, y) = x");
  } else {
    print("min(x, y) = x");
  }
  
  if(y > x){
    print("max(x, y) = y");
  } else {
    print("min(x, y) = y");
  }
}